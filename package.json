{
  "name": "sonic-ai-v3",
  "version": "0.1.0",
  "private": true,
  "description": "Sonic AI monorepo for producer-focused audio asset management and workflow intelligence.",
  "packageManager": "pnpm@10.28.1",
  "scripts": {
    "dev": "pnpm --parallel --recursive dev",
    "build": "pnpm --recursive build",
    "lint": "pnpm --recursive lint",
    "test": "pnpm --recursive test",
    "typecheck": "pnpm --recursive typecheck"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  }
}
