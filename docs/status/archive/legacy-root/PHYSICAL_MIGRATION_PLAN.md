# Physical Migration Plan

The compatibility-safe tree rewrite will preserve original blob SHAs in `docs/status/archive/legacy-root/<same filename>` and replace historical root report blobs with a common compatibility alias notice. This keeps provenance while preventing stale reports from presenting as current state.
