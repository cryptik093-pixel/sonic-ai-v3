# Gate B Root Migration Completion Record

The migration map is authoritative for Gate B. Historical report content is preserved under this directory, and root report paths are compatibility-only.

Physical path normalization must never change the meaning or byte content of archived evidence. Any future move should preserve provenance and retain a compatibility pointer.
