# Physical Migration Pending

The Gate B authority model, lifecycle rules, migration map, and compatibility policy are locked. This marker exists only to prevent any claim that byte-for-byte relocation of every legacy root report has been completed before the tree rewrite is verified.

Remove this marker only after archived copies and root aliases have both been verified.
